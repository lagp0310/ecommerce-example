import { StoreEdit } from "@/components/stores";
import React from "react";

export default function StoreEditPage() {
  return (
    <React.Suspense>
      <StoreEdit />
    </React.Suspense>
  );
}
